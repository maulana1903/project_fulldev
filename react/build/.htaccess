<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /react_app/

  # jangan rewrite file & folder asli
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d

  # semua route lempar ke index.html
  RewriteRule . index.html [L]
</IfModule>
